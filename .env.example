# Aletheia Configuration Template
# 
# QUICK START: Run './dev setup' for automatic secure configuration
# Or copy this file to .env and replace ALL CHANGE_ME values below

# ============================================
# REQUIRED - Must be set for services to start
# ============================================

# Project Configuration
COMPOSE_PROJECT_NAME=aletheia_development
ENVIRONMENT=development

# Database (PostgreSQL) - MUST CHANGE these defaults!
DB_USER=aletheia
DB_PASSWORD=CHANGE_ME_USE_32_CHARS_Min!  # Generate: openssl rand -hex 32
DB_NAME=aletheia
DB_HOST=db
DB_PORT=5432

# Security Keys - MUST CHANGE these defaults!
N8N_ENCRYPTION_KEY=CHANGE_ME_USE_32_CHARS_Min!  # Generate: openssl rand -hex 32
N8N_WEBHOOK_ID=c188c31c-1c45-4118-9ece-5b6057ab5177
NEXTAUTH_SECRET=CHANGE_ME_USE_64_CHARS_Minimum!  # Generate: openssl rand -base64 48

# ============================================
# PORTS - Change if you have conflicts
# ============================================

# Core Services
WEB_PORT=8080
WEB_HOST=0.0.0.0
N8N_PORT=8100
AI_PORTAL_PORT=8102
COURT_PROCESSOR_PORT=8104
POSTGRES_PORT=8200
REDIS_PORT=8201
REVERSE_PROXY_PORT=8082
RECAP_WEBHOOK_PORT=5001

# ============================================
# DOCKER SETTINGS - Platform-specific configurations
# ============================================

# Docker UID/GID for tmpfs mounts (auto-detected by setup)
# On macOS: typically 501 or higher
# On Linux: typically 1000
# Run 'id -u' and 'id -g' to find your values
DOCKER_UID=1000
DOCKER_GID=1000

# ============================================
# SERVICE URLs - Used for inter-service communication
# ============================================

COURT_API_BASE_URL=http://court-processor:8104
NEXT_PUBLIC_COURT_API_URL=http://localhost:8104
NEXT_PUBLIC_WEB_URL=http://localhost:8080
NEXT_PUBLIC_AI_PORTAL_URL=http://localhost:8102
NEXT_PUBLIC_N8N_URL=http://localhost:8100

# ============================================
# OPTIONAL - N8N API Credentials
# Only needed if using n8n's API features (not required for basic operation)
# ============================================

N8N_API_KEY=
N8N_API_SECRET=

# ============================================
# OPTIONAL FEATURES - Leave empty if not using
# ============================================

# AI Service Keys (add your keys if using these services)
# These keys are automatically configured as n8n credentials on startup
# Get your keys from:
# - OpenAI: https://platform.openai.com/api-keys
# - Anthropic: https://console.anthropic.com/settings/keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Document Selection Feature
FIELD_ENCRYPTION_KEY=
NEXT_PUBLIC_ENABLE_DOCUMENT_SELECTION=true
NEXT_PUBLIC_MAX_DOCUMENT_SELECTIONS=5

# PACER Integration
PACER_USERNAME=
PACER_PASSWORD=

# CourtListener API
COURTLISTENER_API_KEY=

# RECAP Webhook
RECAP_WEBHOOK_SECRET=

# Email Configuration (for notifications)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# ============================================
# NOTES
# ============================================
#
# 1. n8n Access: http://localhost:8100
#    Login credentials will be shown when you run './dev up'
#
# 2. For production deployment:
#    - Generate all security keys with provided commands
#    - Use strong database passwords
#    - Enable HTTPS (see docker-compose.production.yml)
#    - Consider using secrets management instead of .env file
#
# 3. Optional services (require additional docker-compose files):
#    - Elasticsearch/Haystack: docker-compose.haystack.yml
#    - BitNet: docker-compose.bitnet.yml
#    - Doctor: docker-compose.doctor.yml
#    - Monitoring: docker-compose.production.yml